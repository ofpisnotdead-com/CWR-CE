# CI test trigger

Empty marker file used to force a second CI run on PR #97 (Windows ccache
experiment) without amending the first run's commit, so both runs can be
compared: cold cache vs. warm cache.
